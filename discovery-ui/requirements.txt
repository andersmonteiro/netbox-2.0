flask>=3.0,<4
gunicorn>=22.0
pynetbox>=7.4,<8
# Coleta via SSH usando Netmiko puro (sem NAPALM) -- device_type
# resolvido pelo Fabricante do device (ver
# discovery_core.resolve_ssh_device_type()). Também usado pra uma
# consulta SSH extra e opcional em devices MikroTik descobertos via
# SNMP -- RouterOS não expõe o vínculo VLAN -> porta física por SNMP de
# leitura simples, então lemos via CLI quando o operador informa uma
# credencial SSH extra (ver discovery_core.collect_mikrotik_interface_details()).
netmiko>=4.3
# Templates TextFSM usados pelo Netmiko (send_command(..., use_textfsm=
# True)) pra estruturar a saída de comandos "show"/"display" por
# fabricante -- ver discovery_core._ssh_collect_textfsm().
ntc-templates>=6.0
python-dotenv>=1.0
# Cifra a senha de descoberta (discovery_password) antes de gravar no
# NetBox -- ver encrypt_secret()/decrypt_secret() em discovery_core.py.
# Já vem transitivamente via paramiko (dependência do netmiko), mas
# fica explícito aqui por clareza e pra não depender de um detalhe de
# implementação de outra lib continuar puxando ela no futuro.
cryptography>=42
# Aba "Serviços" do navbar (status ligado/parado + botão de reiniciar dos
# containers do NetBox) -- fala com o Docker do host via socket montado
# em /var/run/docker.sock (ver _get_docker_client() em app.py e o
# volume no docker-compose.*.yml). Se o socket não estiver montado, o
# client falha ao conectar e a aba cai pra "indisponível" sem quebrar o
# resto da aplicação.
docker>=7.0
